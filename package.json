{
  "name": "sessionmap",
  "version": "0.1.0-beta.3",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "license": "MIT",
  "author": "Hao",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/hao-cyber/session-map.git"
  },
  "homepage": "https://github.com/hao-cyber/session-map#readme",
  "bugs": "https://github.com/hao-cyber/session-map/issues",
  "keywords": [
    "ai-agents",
    "claude-code",
    "codex",
    "developer-tools",
    "thinking-tree",
    "session-management"
  ],
  "type": "module",
  "description": "A persistent external thinking tree for parallel AI coding sessions",
  "bin": {
    "sessionmap": "apps/runtime/src/cli.ts"
  },
  "files": [
    "apps/runtime/src",
    "apps/web/src",
    "apps/desktop/src",
    "packages/core/src",
    "docs",
    "README.md",
    "LICENSE",
    "THIRD_PARTY_NOTICES.md"
  ],
  "scripts": {
    "dev": "SESSIONMAP_DEV=1 bun --watch apps/runtime/src/cli.ts serve",
    "start": "bun apps/runtime/src/cli.ts serve",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "check:web": "bun run scripts/check-web.ts",
    "check:privacy": "bun run scripts/check-privacy.ts",
    "check:secrets": "bun run scripts/check-gitleaks.ts",
    "check:ci": "bun run check:secrets && bun run check",
    "build": "bun run scripts/build.ts",
    "build:app": "bun run scripts/build-macos-app.ts",
    "check:app": "swiftc -parse-as-library -typecheck -framework AppKit -framework WebKit apps/desktop/src/SessionMapApp.swift && plutil -lint apps/desktop/src/Info.plist >/dev/null",
    "prepublishOnly": "bun run check",
    "check:release": "sh -n scripts/install.sh scripts/macos/postinstall && plutil -lint scripts/macos/entitlements.plist apps/desktop/src/Info.plist >/dev/null && xmllint --noout scripts/macos/Distribution.xml",
    "check": "bun run typecheck && bun run check:web && bun run check:privacy && bun run check:app && bun run check:release && bun test && bun run build && bun run build:app"
  },
  "devDependencies": {
    "@types/bun": "^1.3.13",
    "lucide-static": "^1.24.0",
    "markmap-lib": "^0.18.12",
    "typescript": "^5.9.3"
  },
  "engines": {
    "bun": ">=1.3.13"
  },
  "packageManager": "bun@1.3.13"
}
